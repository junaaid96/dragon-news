import React from 'react';
import { Link } from 'react-router-dom';

const Header = () => {
    return (
        <div className='d-flex'>
            <h1>Logo</h1>
        </div>
    );
};

export default Header;